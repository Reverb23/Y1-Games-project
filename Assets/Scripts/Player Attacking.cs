using UnityEngine;

public class PlayerAttacking : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    int PlayerLvl = PlayerStats.PlayerLevel;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        print(PlayerLvl);
    }
    void AngleFinder()
    // find the angle that the projectiles will leave the player at
    {



    }
}
